name: Dev branch Checks

on:
  push:
    branches: ['*', '!testing', '!main', '!next']
  pull_request:
    branches: ['*']

permissions:
  contents: read

jobs:
  test:
    name: Tests & Lints
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.7.13

      - name: Install dependencies
        run: deno install --allow-scripts

      - name: Astro & Typescript checks
        run: deno task check

      - name: Deno linter
        run: deno lint

      - name: ESLint
        run: deno task lint

      - name: Check formatting
        run: deno task format:diff
